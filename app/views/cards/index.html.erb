<h1>Cards#index</h1>
<p>※現在、登録されているデータの一覧です。</p>
<table width='100%'>
  <tr>
    <th>ID</th><th>題名</th><th>著者</th><th colspan='2'></th>
  </tr>
  <% @cards.each do |card| %>
    <tr>
      <td width='35px'><%= card.id %></td>
      <td width='50%'><%= link_to(card.title, card_path(card)) %></td>
      <td><%= card.author %></td>
      <td width='40px'><%= link_to('編集', edit_card_path(card)) %></td>
      <td width='40px'><%= link_to('削除', card_path(card), method: :delete, data: { confirm: "#{card.title}のデータを削除しますか？" }) %></td>
    </tr>
  <% end %>
</table>
<p class="link"><%= link_to('※新しいデータを入力', new_card_path) %></p>
