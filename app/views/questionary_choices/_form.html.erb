<%= form_for(questionary_choice) do |f| %>
  <% if questionary_choice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(questionary_choice.errors.count, "error") %> prohibited this questionary_choice from being saved:</h2>

      <ul>
      <% questionary_choice.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.hidden_field :value, { value: @questionary_choice_value } %>
  <p>選択肢番号：<%= @questionary_choice_value %></p>
  
  <div class="field">
    <%= f.label :content %>
    <%= f.text_area :content %>
  </div>
  
  <%= f.hidden_field :questionary_item_id, { value: @questionary_choice.questionary_item_id } %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
